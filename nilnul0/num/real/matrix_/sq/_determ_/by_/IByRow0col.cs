namespace nilnul.num.real.matrix_.sq._determ_.by_
{
	/// en.wikipedia.org/wiki/Laplace_expansion
	/// In linear algebra, the Laplace expansion, named after Pierre-Simon Laplace, 
	/// <summary>
	/// also called cofactor expansion, is an expression of the determinant of an n × n-matrix B as a weighted sum of minors, which are the determinants of some (n − 1) × (n − 1)-submatrices 
	/// :
	///		(-1)**(i+j) m[i,j] minor[i,j]
	/// 
	/// </summary>
	/// <remarks>
	/// For large matrices, it quickly becomes inefficient to compute when compared to Gaussian elimination
	/// </remarks>
	/// <see cref="real.matrix.IRow0col"/>
	/// alias:
	///		Laplace expansion of one col or one row;
	public interface IByRow0col
	{
	}



}
