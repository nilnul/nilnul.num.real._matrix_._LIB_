namespace nilnul.num.real.matrix
{
	///<see cref="real.matrix.to_.factorize_.ISingularValueDecomposition"/>
	/// 
	/// <summary>
	/// when <see cref="real.matrix.op_.sym_.IEndoMul"/> is <see cref="real.matrix_.sq_.sym_.definite_.INonneg"/>, then the sqrt(eigenval) is called singular value. The values are the same when sorted, no matter whether it is <see cref="real.matrix.op_.sym_.endomul_.IMulTransposed"/> or <see cref="real.matrix.op_.sym_.endomul_.ITransposedMul"/>
	/// </summary>
	public interface ISingularValues
	{
	}


}
