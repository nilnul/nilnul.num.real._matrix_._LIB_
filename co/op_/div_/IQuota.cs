namespace nilnul.num.real.matrix.co.op_.div_
{
	/// <see cref="IPer"/>
	/// <summary>
	/// A\B
	///		,where A\1 is the left inverse of A
	/// we need to get the <see cref="num.real.matrix_.sq.op_._invert.ISemiInvert"/>
	/// </summary>
	/// <see cref="num.quotient.matrix.co"/>
	/// <see cref="num.real.matrix.pred_.rudic_.col_.equat._slv_.by_._ByRowReductionX"/>
	/// alias:
	///		q, visually, like \
	///			,while as p is visually like /
	public interface IQuota
	{
	}

}
