namespace nilnul.num.real.matrix.op_.basic_
{
	/// <see cref="num.real.matrix_.sq_.diag_.dilute_"/>
	/// <see cref="matrix.op_._indent._GaussGramX._Shed_2divisorUnish(ref quotient_.DenomNonnil[,], int, quotient_.DenomNonnil)"/>
	/// <summary>
	/// in align with <see cref="real.matrix_.sq_.basic_._ShedX._Shed_0positive_1row(int, int, quotient_.DenomNonnil)"/> where the par is placed in the cel, and when mulitiplied, acts as a scalar.
	/// 
	/// </summary>
	/// <remarks>
	/// we can do division in <see cref="matrix.IOp"/>, not the <see cref="real.matrix_.sq_.basic_.IShed"/>, by explicitly noting it is a divisor;
	/// to divide only one row by a number, which has to be unnil as it is now a divisor.
	/// eg:
	///		[ 0 , 2, 3]
	///		=> [ 0, 1,3/2]
	/// 
	/// </remarks>
	/// alias:
	///		shaft
	///		spread
	///		snowball
	///		strengthen
	///		strong
	///		
	///		shy
	///		shed, four letters like <see cref="op_.ISwap"/>
	///			,dilute
	///			, if the scalar is small
	///		shred
	///		
	///		stretch, in 1dimension, not all dimensions as in <see cref="real.matrix.op_.IShrink"/>
	///		stray
	///		sift
	///		<see cref="op_._indent.gauss_.IShed"/>
	///		transvections	
	///		shear
	/// 
	///	vs:
	///		divide all cels:<see cref="op_.IShrink"/>
	///		skew,<see cref="ISkew"/>
	///		<see cref="matrix.row.op_.IShed"/>
	///		
	public interface IShed
		:
		squash_.IShed
		,
		matrix.row.op_.IShed //.IOp
	{
	}



}
