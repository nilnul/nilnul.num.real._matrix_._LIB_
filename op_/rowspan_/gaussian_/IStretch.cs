namespace nilnul.num.real.matrix.op_.rowspan_.gaussian_
{
	/// vs:
	///		<see cref="op_."/>
	///
	/// <summary>
	/// scale on only one row.
	/// visually, this is widen or heighten one dimension.
	/// </summary>
	/// alias:
	///		spring
	///		spread
	///		stretch
	///			,longest word in stetch, slant,  swap, and the simplest (only one axis) operation. slant is simpler than swap in that it doesnot flip the enclosure/winding/signed area.
	public interface IStretch:IGaussian
    {
    }



}
