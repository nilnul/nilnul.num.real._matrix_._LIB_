using System.Collections;

namespace nilnul.num.real.matrix.co_.multible.multi_
{
	/// <summary>
	/// split the left, and the length of the resulted would change
	/// split the right, and the arity of the vec of the resulted would change.
	/// either left or right are free to split independently.
	/// </summary>
	/// <remarks>
	/// <see cref="num.real.vec.str.co._MatrixMultiX"/>
	/// </remarks>
	/// alias:
	///		vecs1vecs
	///		bySplit

	public interface IVecs1vecs
		:IByGrid
	{ }
}
